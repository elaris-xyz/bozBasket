[toolchain]
anchor_version = "0.30.1"

[features]
resolution = true
skip-lint = false

# Placeholder id. After the first `anchor build`, run `anchor keys sync`
# so this and declare_id!() both point at target/deploy/basket_dca-keypair.json.
[programs.localnet]
basket_dca = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[programs.devnet]
basket_dca = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "pnpm exec ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[workspace]
members = ["programs/basket_dca"]
